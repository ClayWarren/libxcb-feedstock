{% set xorg_name = "libxcb" %}
{% set xorg_category = "lib" %}
{% set name = "libxcb" %}
{% set version = "1.17.0" %}
{% set sha256 = "2c69287424c9e2128cb47ffe92171e10417041ec2963bceafb65cb3fcf8f0b85" %}
{% set am_version = "1.15" %} # keep synchronized with build.sh

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://xorg.freedesktop.org/archive/individual/{{ xorg_category }}/{{ xorg_name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    - windows.patch  # [win and not arm64]
    - native-clang.patch  # [win and arm64]

build:
  number: 3
  detect_binary_files_with_prefix: true
  run_exports:
    - {{ pin_subpackage('libxcb', max_pin='x') }}

requirements:
  build:
    - python                        # [build_platform != target_platform and not (win and arm64)]
    - python >=3.14                 # [win and arm64]
    - xcb-proto {{ version }}       # [build_platform != target_platform or (win and arm64)]
    # Keep the x64 MinGW toolchain; ARM64 uses native Clang with the MSVC runtime.
    - {{ stdlib("c") }}
    - {{ compiler("c") }}
    - m2-autoconf                   # [win and not arm64]
    - m2-automake{{ am_version }}   # [win and not arm64]
    - m2-libtool                    # [win and not arm64]
    - pkg-config                    # [unix or arm64]
    - xorg-util-macros               # [win and arm64]
    - m2-pkg-config                 # [win and not arm64]
    - gnuconfig                     # [unix or arm64]
    - m2-base                       # [win]
    - make                          # [unix]
    - m2-make                       # [win]
    - autoconf                      # [unix]
    - automake                      # [unix]
    - gettext                       # [unix]
    - libtool                       # [unix]
  host:
    - winpthreads                   # [win and arm64]
    - pthread-stubs
    - xorg-libxau
    - xorg-libxdmcp
    - xorg-util-macros              # [not (win and arm64)]
    - xcb-proto {{ version }}       # [not (win and arm64)]
    - xorg-xorgproto
  run:
    - libwinpthread                 # [win and arm64]
    - winpthreads                   # [win and arm64]
    - xorg-xorgproto                # [win and arm64]
    - pthread-stubs
    - xorg-libxau
    - xorg-libxdmcp

test:
  requires:
    - python >=3.14  # [win and arm64]
    - {{ compiler('c') }}  # [win and arm64]
    - {{ stdlib('c') }}  # [win and arm64]
  files:
    - test-native.c  # [win and arm64]
    - test-native.bat  # [win and arm64]
    - test-protocol.py  # [win and arm64]
  commands:
    - test -f $PREFIX/lib/libxcb.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-composite.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-composite.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-composite.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-composite-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-damage.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-damage.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-damage.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-damage-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-dpms.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-dpms.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-dpms.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-dpms-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-dri2.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-dri2.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-dri2.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-dri2-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-glx.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-glx.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-glx.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-glx-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-present.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-present.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-present.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-present-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-randr.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-randr.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-randr.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-randr-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-record.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-record.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-record.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-record-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-res.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-res.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-res.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-res-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-screensaver.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-screensaver.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-screensaver.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-screensaver-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-shape.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-shape.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-shape.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-shape-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-shm.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-shm.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-shm.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-shm-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-sync.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-sync.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-sync.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-sync-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-xf86dri.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-xf86dri.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-xf86dri.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-xf86dri-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-xfixes.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-xfixes.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-xfixes.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-xfixes-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-xinerama.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-xinerama.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-xinerama.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-xinerama-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-xkb.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-xkb.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-xkb.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-xkb-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-xtest.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-xtest.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-xtest.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-xtest-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-xv.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-xv.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-xv.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-xv-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-xvmc.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-xvmc.so  # [linux]
    - if not exist %PREFIX%/Library/lib/libxcb-xvmc.dll.a exit /b 1  # [win and not arm64]
    - if not exist %PREFIX%/Library/bin/msys-xcb-xvmc-*.dll exit /b 1  # [win and not arm64]
    - test -f $PREFIX/lib/libxcb-dri3.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-dri3.so  # [linux]
    - test -f $PREFIX/lib/libxcb-render.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-render.so  # [linux]
    - test -f $PREFIX/lib/libxcb-xinput.dylib  # [osx]
    - test -f $PREFIX/lib/libxcb-xinput.so  # [linux]
    - call test-native.bat  # [win and arm64]

about:
  home: http://xcb.freedesktop.org/
  license: MIT
  license_family: MIT
  license_file: COPYING
  summary: "This is the C-language Binding (XCB) package to the X Window System protocol"

extra:
  recipe-maintainers:
    - ccordoba12
    - pkgw
